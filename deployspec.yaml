version: 0.2

env:
  variables:
    ENV_NAME: dev

phases:
  pre_build:
    commands:
      - npm ci
  build:
    commands:
      - echo Entered the deploy phase to $ENV_NAME...
      - echo Deploy started
  artifacts:
    baseDirectory: build
    files:
      - "**/*"
